
// Add push notification trigger to existing NotificationService
// Add this method to the class:

/*
static async sendWithPush(notification, userId) {
  try {
    const pushService = require('./pushNotificationService');
    await pushService.sendPushNotification(
      userId,
      notification.title,
      notification.body,
      notification.data
    );
  } catch (error) {
    console.error('Push notification error:', error);
  }
}
*/
